query CartInput {
  cart {
    cost {
      subtotalAmount {
        amount
        currencyCode
      }
    }
    attribute(key: "melonx_points") {
      value
    }
  }
  discount {
    discountClasses
  }
}
